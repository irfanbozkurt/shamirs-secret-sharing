{
  "name": "shamir-secret-share",
  "private": true,
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test --timeout 30000 --bail --coverage",
    "gen": "bun run ./bin/index.ts",
    "split": "leo run split",
    "recover": "leo run recover",
    "split:execute": "leo execute split",
    "recover:execute": "leo execute recover"
  },
  "devDependencies": {
    "bun-types": "latest",
    "prettier": "^3.1.0",
    "typescript": "^5.2.2"
  },
  "prettier": {
    "printWidth": 120
  }
}
